import React, { useState } from 'react'
import { Flex } from '@chakra-ui/react'

export default function Integrations({ user }) {
    const [isOpen, setIsOpen] = useState(false)
    return <Flex mt="48px" w="100%"></Flex>
}
